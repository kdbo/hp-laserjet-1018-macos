name: Build HP LaserJet 1018 Package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-15

    env:
      APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
      APPLE_APPLICATION_IDENTITY: ${{ vars.APPLE_APPLICATION_IDENTITY }}
      APPLE_INSTALLER_IDENTITY: ${{ vars.APPLE_INSTALLER_IDENTITY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [[ "$GITHUB_REF_TYPE" != "tag" ]]; then
            echo "ERROR: This workflow must be run from a version tag."
            exit 1
          fi

          VERSION="${GITHUB_REF_NAME#v}"

          if [[ -z "$VERSION" ]]; then
            echo "ERROR: Empty version."
            exit 1
          fi

          echo "Git tag: $GITHUB_REF_NAME"
          echo "Package version: $VERSION"

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Show build environment
        run: |
          sw_vers
          uname -m
          clang --version
          pkgbuild --version
          productsign --version
          codesign --version

      # ------------------------------------------------------------
      # Import Developer ID certificates into a temporary keychain
      # ------------------------------------------------------------

      - name: Create temporary signing keychain
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/hp1018-signing.keychain-db"

          security create-keychain \
            -p "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"

          security set-keychain-settings \
            -lut 21600 \
            "$KEYCHAIN_PATH"

          security unlock-keychain \
            -p "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"

          security list-keychains \
            -d user \
            -s "$KEYCHAIN_PATH"

          security default-keychain \
            -s "$KEYCHAIN_PATH"

      - name: Import Developer ID Application certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.MACOS_APPLICATION_P12 }}
          CERTIFICATE_PASSWORD: ${{ secrets.MACOS_APPLICATION_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/hp1018-signing.keychain-db"
          CERTIFICATE_PATH="$RUNNER_TEMP/application.p12"

          echo "$CERTIFICATE_BASE64" | base64 --decode > "$CERTIFICATE_PATH"

          security import "$CERTIFICATE_PATH" \
            -P "$CERTIFICATE_PASSWORD" \
            -k "$KEYCHAIN_PATH" \
            -T /usr/bin/codesign \
            -T /usr/bin/productsign

          rm -f "$CERTIFICATE_PATH"

      - name: Import Developer ID Installer certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.MACOS_INSTALLER_P12 }}
          CERTIFICATE_PASSWORD: ${{ secrets.MACOS_INSTALLER_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/hp1018-signing.keychain-db"
          CERTIFICATE_PATH="$RUNNER_TEMP/installer.p12"

          echo "$CERTIFICATE_BASE64" | base64 --decode > "$CERTIFICATE_PATH"

          security import "$CERTIFICATE_PATH" \
            -P "$CERTIFICATE_PASSWORD" \
            -k "$KEYCHAIN_PATH" \
            -T /usr/bin/codesign \
            -T /usr/bin/productsign

          rm -f "$CERTIFICATE_PATH"

      - name: Configure keychain access
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/hp1018-signing.keychain-db"

          security set-key-partition-list \
            -S apple-tool:,apple: \
            -s \
            -k "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"

      - name: Verify signing identities
        run: |
          security find-identity -v -p codesigning
          security find-identity -v -p basic

      # ------------------------------------------------------------
      # Build x86_64 and arm64 versions
      # ------------------------------------------------------------

      - name: Build rastertozjs x86_64
        run: |
          clang -arch x86_64 \
            -o rastertozjs-x86_64 \
            rastertozjs.c \
            foo2zjs/jbig.c \
            foo2zjs/jbig_ar.c \
            -Ifoo2zjs \
            -lcups \
            -lcupsimage \
            -Wall \
            -O2

      - name: Build rastertozjs arm64
        run: |
          clang -arch arm64 \
            -o rastertozjs-arm64 \
            rastertozjs.c \
            foo2zjs/jbig.c \
            foo2zjs/jbig_ar.c \
            -Ifoo2zjs \
            -lcups \
            -lcupsimage \
            -Wall \
            -O2

      - name: Create universal binary
        run: |
          lipo -create \
            rastertozjs-x86_64 \
            rastertozjs-arm64 \
            -output rastertozjs-universal

          file rastertozjs-universal
          lipo -info rastertozjs-universal

      # ------------------------------------------------------------
      # Sign application
      # ------------------------------------------------------------

      - name: Sign rastertozjs
        run: |
          codesign \
            --force \
            --options runtime \
            --timestamp \
            --sign "$APPLE_APPLICATION_IDENTITY" \
            rastertozjs-universal

      - name: Verify rastertozjs signature
        run: |
          codesign -dvvv rastertozjs-universal 2>&1

          codesign \
            --verify \
            --strict \
            --verbose=4 \
            rastertozjs-universal

          codesign \
            --verify \
            --strict \
            --verbose=4 \
            --arch x86_64 \
            rastertozjs-universal

          codesign \
            --verify \
            --strict \
            --verbose=4 \
            --arch arm64 \
            rastertozjs-universal

      # ------------------------------------------------------------
      # Assemble package payload
      # ------------------------------------------------------------

      - name: Prepare package build tree
        run: |
          rm -rf pkg-build

          mkdir -p \
            pkg-build/root/usr/libexec/cups/filter \
            pkg-build/root/usr/local/libexec/hp1018 \
            pkg-build/root/usr/local/share/hp1018 \
            pkg-build/root/Library/LaunchDaemons \
            "pkg-build/root/Library/Application Support/HP1018"

          cp rastertozjs-universal \
            pkg-build/root/usr/libexec/cups/filter/rastertozjs

          chmod 755 \
            pkg-build/root/usr/libexec/cups/filter/rastertozjs

      # ------------------------------------------------------------
      # Copy project-specific runtime files
      #
      # These files must exist in the repository:
      #
      #   pkg-build/root/usr/local/libexec/hp1018/watch.sh
      #   pkg-build/root/usr/local/share/hp1018/sihp1018.dl
      #   pkg-build/root/Library/LaunchDaemons/...
      #   pkg-build/root/Library/Application Support/HP1018/airprint.sh
      #
      # ------------------------------------------------------------

      - name: Verify package source files
        run: |
          test -f pkg-build/root/usr/local/libexec/hp1018/watch.sh
          test -f pkg-build/root/usr/local/share/hp1018/sihp1018.dl
          test -f pkg-build/root/Library/LaunchDaemons/com.kdbo.hp1018-firmware.plist
          test -f pkg-build/root/Library/LaunchDaemons/com.kdbo.hp1018.airprint.plist
          test -f "pkg-build/root/Library/Application Support/HP1018/airprint.sh"

      # ------------------------------------------------------------
      # Build unsigned installer package
      # ------------------------------------------------------------

      - name: Build package
        run: |
          pkgbuild \
            --root pkg-build/root \
            --scripts pkg-build/scripts \
            --identifier com.kdbo.hp1018 \
            --version "${{ steps.version.outputs.version }}" \
            --install-location / \
            "HP1018-${{ steps.version.outputs.version }}.pkg"

      - name: Inspect package
        run: |
          pkgutil --check-signature "HP1018-{{ steps.version.outputs.version }}.pkg" || true
          pkgutil --expand-full \
            "HP1018-{{ steps.version.outputs.version }}pkg" \
            /tmp/hp1018-package-check

          file \
            /tmp/hp1018-package-check/Payload/usr/libexec/cups/filter/rastertozjs

      # ------------------------------------------------------------
      # Sign installer package
      # ------------------------------------------------------------

      - name: Sign installer package
        run: |
          productsign \
            --sign "$APPLE_INSTALLER_IDENTITY" \
            "HP1018-{{ steps.version.outputs.version }}.pkg" \
            "HP1018-{{ steps.version.outputs.version }}-signed.pkg"

      - name: Verify installer signature
        run: |
          pkgutil --check-signature \
            "HP1018-{{ steps.version.outputs.version }}-signed.pkg"

      # ------------------------------------------------------------
      # Upload package as GitHub Actions artifact
      # ------------------------------------------------------------

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: HP1018-{{ steps.version.outputs.version }}
          path: HP1018-{{ steps.version.outputs.version }}-signed.pkg

      # ------------------------------------------------------------
      # Cleanup
      # ------------------------------------------------------------

      - name: Remove temporary signing keychain
        if: always()
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/hp1018-signing.keychain-db"

          security delete-keychain "$KEYCHAIN_PATH" 2>/dev/null || true